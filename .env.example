# Public browser build setting. Rebuild the frontend after changing it.
VITE_BACKEND_URL=http://localhost:8000

# Comma-separated browser origins accepted by the backend.
CORS_ORIGINS=http://localhost:5173
